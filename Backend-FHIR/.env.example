# Claude API
ANTHROPIC_API_KEY=sk-ant-api03-...

# InterSystems IRIS — hackathon kit defaults (docker-compose)
IRIS_HOST=localhost
IRIS_PORT=32782          # native protocol port (Python driver)
IRIS_NAMESPACE=DEMO
IRIS_USER=_SYSTEM
IRIS_PASSWORD=ISCDEMO

# FHIR endpoint — set automatically from above, override if needed
# IRIS_FHIR_URL=http://localhost:32783/csp/healthshare/demo/fhir/r4
